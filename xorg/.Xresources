
! Colours
xterm*termName: xterm-256color
xterm*background: Black
xterm*foreground: grey

! Font
xterm*faceName: IBM Plex Mono
xterm*faceSize: 10
xterm*renderFont: true

! VT Font Menu: Unreadable
xterm*faceSize1: 8
! VT font menu: Tiny
xterm*faceSize2: 10
! VT font menu: Medium
xterm*faceSize3: 12
! Large and Huge seem to be backwards or could be an issue with IBM Plex
! VT font menu: Large
xterm*faceSize4: 14
! VT font menu: Huge
xterm*faceSize5: 16

! Key bindings
xterm*VT100.Translations: #override \
    Ctrl <Key> minus: smaller-vt-font() \n\
    Ctrl <Key> plus: larger-vt-font() \n\
    Ctrl <Key> 0: set-vt-font(d) \n\
    Shift <KeyPress> Insert: insert-selection(CLIPBOARD) \n\
    Ctrl Shift <Key>V:    insert-selection(CLIPBOARD) \n\
    Ctrl Shift <Key>C:    copy-selection(CLIPBOARD) \n\
    Alt <Btn1Up>: exec-formatted("xdg-open '%t'", PRIMARY)

! Not sure if this is possible yet
! Ctrl Shift <Key>U: dump-html(); exec-formatted("xdg-open $(rofi-one-liner-goes-here)", PRIMARY)

! Dunno what key to bind this too
! Rofi url picker
! Need to trigger the dump-html() feature
! And then somehow run this:
! xdg-open $(grep -oE '(ftp|https)://[^\s<>]*' ~/xterm.<datetime>.xhtml | rofi -dmenu)
